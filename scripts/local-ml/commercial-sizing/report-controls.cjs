'use strict';

const controlsCss=`
.mode-picker-label{font-size:14px;font-weight:800;margin:0 0 10px;color:#365345}.report-modes .btn{text-align:left;padding:12px 16px}.report-modes .btn span,.report-modes .btn small{display:block}.report-modes .btn small{font-size:12px;font-weight:500;margin-top:5px;opacity:.8}.model-error-explanation{margin-top:24px;padding-top:20px;border-top:1px solid #cdded3}.model-error-explanation h3{font-size:20px;margin:0 0 10px}.model-error-explanation p{line-height:1.6}.model-error-explanation td small{display:block;color:#617269;font-size:12px;margin-top:4px}.model-error-explanation .example{margin-top:18px;border:1px solid #cdded3;border-radius:12px;padding:16px;background:#f8fcf9}.model-error-explanation .example h4{margin:0 0 8px}.model-error-explanation .example p{font-size:13px;margin:8px 0}
.quick-report{border:2px solid #c6ded1;background:#fff;margin-top:18px}.quick-report h2{font-size:28px!important;margin:0 0 6px}.quick-intro{margin:0 0 18px;color:#586b61}.quick-controls{display:grid;grid-template-columns:repeat(auto-fit,minmax(175px,1fr));gap:12px;align-items:end}.quick-controls label{display:block;font-size:12px;font-weight:750;color:#465e52}.quick-controls input{display:block;width:100%;border:1px solid #cad9d0;border-radius:12px;background:#fff;padding:12px;margin-top:6px;min-height:47px}.custom-select{position:relative;margin-top:6px;font-weight:600}.custom-select-trigger{width:100%;display:flex;align-items:center;justify-content:space-between;gap:12px;border:1px solid #cad9d0;background:white;border-radius:12px;padding:12px 13px;min-height:47px;text-align:left;font:inherit;color:#18372a;cursor:pointer}.custom-select-trigger:hover,.custom-select-trigger[aria-expanded=true]{border-color:#2f8a68;box-shadow:0 0 0 3px #e0f1e7}.custom-select-trigger:focus-visible,.custom-option:focus-visible{outline:3px solid #90cbb0;outline-offset:2px}.custom-select-chevron{font-size:11px;opacity:.7}.custom-select-menu{position:absolute;top:calc(100% + 7px);left:0;right:0;z-index:40;min-width:100%;padding:6px;background:#fff;border:1px solid #c4d6ca;border-radius:13px;box-shadow:0 16px 36px #173e2929;max-height:300px;overflow:auto}.custom-option{display:flex;justify-content:space-between;width:100%;border:0;background:transparent;padding:11px 10px;border-radius:8px;text-align:left;font:inherit;color:#263e31;cursor:pointer}.custom-option:hover,.custom-option:focus{background:#edf6f0}.custom-option[aria-selected=true]{background:#dff0e6;color:#105638;font-weight:800}.custom-option:disabled{opacity:.45;cursor:default}.quick-summary{margin-top:20px}.quick-context{margin:0 0 14px;font-size:14px}.quick-model-grid{display:grid;grid-template-columns:1fr 1fr;gap:16px}.quick-card{border:1px solid #cdded3;border-radius:18px;padding:22px;background:#f8fcf9}.quick-card.our{background:#f2f6fc;border-color:#cddbeb}.quick-card h3{margin:0 0 10px;font-size:17px}.quick-main{font-size:44px;line-height:1.1;font-weight:850;letter-spacing:-.04em;color:#145c43}.quick-card.our .quick-main{color:#294f83}.quick-main-label{font-weight:750;margin:8px 0 4px}.quick-denominator{font-size:12px;color:#586b61;margin:0 0 16px}.quick-breakdown{display:grid;grid-template-columns:repeat(3,1fr);gap:8px;margin-top:12px}.quick-breakdown div{background:#ffffffb8;border:1px solid #dae5de;border-radius:10px;padding:10px}.quick-breakdown strong,.quick-breakdown span{display:block}.quick-breakdown strong{font-size:20px}.quick-breakdown span{font-size:11px;color:#52665a;margin-top:3px}.quick-bar{height:9px;display:flex;border-radius:10px;overflow:hidden;background:#e4eae6}.quick-bar span{height:100%;display:block}.quick-bar .same{background:#2f8a68}.quick-bar .up{background:#d5a548}.quick-bar .down{background:#678abb}.quick-bar .unavailable{background:#ba6464}.quick-takeaway{padding:15px 17px;border-radius:12px;background:#e9f3ed;color:#173d29;margin-top:14px;font-weight:650}.quick-note{font-size:12px;color:#617269;margin:12px 0 0}.quick-section-label{font-size:13px;font-weight:800;letter-spacing:.03em;margin:24px 0 10px}.quick-empty{padding:25px;background:#f4f7f5;border-radius:12px;text-align:center;color:#586b61}.quick-table-filters{display:flex;gap:12px;align-items:end;flex-wrap:wrap;margin:16px 0}.quick-table-filters label{min-width:180px;display:block;font-size:12px;font-weight:700}.quick-table-note{font-size:12px;color:#68776e;margin:8px 0}.full-results{margin-top:22px}.full-results>summary{cursor:pointer;background:#f7faf8;border:1px solid #cbded1;border-radius:12px;padding:14px 17px;font-weight:750}.full-results[open]>summary{margin-bottom:12px}.full-results .section:first-of-type{margin-top:12px}
.quick-result.has-explanation{display:grid;grid-template-columns:minmax(190px,.8fr) minmax(260px,1.2fr);gap:24px;align-items:start;margin-bottom:18px}.quick-check-explanation{border-left:1px solid #cdded3;padding-left:22px;font-size:13px;line-height:1.55;color:#465e52}.quick-check-explanation p{margin:0 0 10px}.quick-check-explanation p:last-child{margin-bottom:0}.quick-check-explanation strong{color:#18372a}
.quick-outcome-help{font-size:12px;line-height:1.5;color:#465e52;margin:9px 0 0}
#mode-error.is-simulation .real-chart-only{display:none}
.gap-simulation-settings{margin-top:16px;padding:16px;border:1px solid #dfc997;border-radius:12px;background:#fffaf0}.gap-simulation-settings>p{margin:0 0 12px;font-size:13px;line-height:1.5}.gap-controls{display:grid;grid-template-columns:1fr 1fr 1fr auto;gap:12px;align-items:end}.gap-controls label{font-size:12px;font-weight:750}.gap-controls input{display:block;min-height:47px;width:100%;margin-top:6px;padding:12px;border:1px solid #cad9d0;border-radius:12px;background:white}.gap-controls .btn{min-height:47px}.gap-status{font-size:13px;margin-top:12px}.simulation-label{background:#fff1d3;border:1px solid #e3c17e;border-radius:12px;padding:14px;line-height:1.5;font-size:13px}.simulation-label strong{display:block;margin-bottom:4px}@media(max-width:700px){.gap-controls{grid-template-columns:1fr 1fr}.quick-breakdown{grid-template-columns:1fr}.quick-breakdown div{padding:12px}.quick-outcome-help{margin-top:5px}}
@media(max-width:700px){.quick-model-grid{grid-template-columns:1fr}.quick-controls{grid-template-columns:1fr 1fr}.quick-main{font-size:38px}.quick-report{padding:18px}.quick-breakdown strong{font-size:18px}.quick-result.has-explanation{grid-template-columns:1fr;gap:12px}.quick-check-explanation{border-left:0;border-top:1px solid #cdded3;padding:14px 0 0}}@media(max-width:420px){.quick-controls{grid-template-columns:1fr}}
`;

function installReportControls(data){
  const $=id=>document.getElementById(id);
  const esc=v=>String(v??'N/A').replaceAll('&','&amp;').replaceAll('<','&lt;').replaceAll('>','&gt;').replaceAll('"','&quot;');
  const percent=(n,d)=>d?(100*n/d).toFixed(2)+'%':'N/A';
  const count=n=>n.toLocaleString('en-US');
  const dualUnit=(cm,signed=false)=>{const sign=signed&&cm>0?'+':'';return sign+cm+' cm'+(cm===0?' / 0 in':' ≈ '+sign+(cm/2.54).toFixed(2)+' in');};
  const labelForSelect=select=>select.getAttribute('aria-label')||Array.from(select.labels?.[0]?.childNodes||[]).filter(node=>node.nodeType===3).map(node=>node.textContent).join('').trim()||select.id;
  const selectMarkup=(id,label,options)=>'<label>'+label+'<select id="'+id+'">'+options.map(([value,text])=>'<option value="'+value+'">'+text+'</option>').join('')+'</select></label>';
  const categories=[['all','All products'],['pants','Pants / trousers'],['shorts','Shorts'],['skirts','Skirts']];
  const newLabel=(id,label,options)=>{const holder=document.createElement('div');holder.innerHTML=selectMarkup(id,label,options);return holder.firstElementChild;};
  for(const mode of ['cohort','error']){
    const panel=$('mode-'+mode),toolbar=panel.querySelector('.toolbar'),filters=panel.querySelector('.filters');
    const quick=document.createElement('section');quick.id=mode+'-quick-report';quick.className='section quick-report';
    quick.innerHTML='<h2>Your selected report</h2><p class="quick-intro">'+(mode==='error'?'Choose the tape error and products. See how often the product size stays the same.':'Choose people and products. Compare Aiad and our model immediately.')+'</p><div class="quick-controls"></div><div id="'+mode+'-quick-summary" class="quick-summary" aria-live="polite"></div>';
    toolbar.after(quick);
    const controls=quick.querySelector('.quick-controls');
    for(const id of mode==='error'?['error-axis','error-delta','error-gender']:['cohort-gender'])controls.appendChild($(id).closest('label'));
    controls.appendChild(newLabel(mode+'-category','Products',categories));
    if(mode==='error')controls.appendChild(newLabel('error-chart-mode','Size charts',[['real','Real product charts'],['simulated','Made-up size gap']]));
    controls.appendChild($(mode+'-search').closest('label'));
    if(mode==='error'){
      const settings=document.createElement('div');settings.className='gap-simulation-settings';settings.id='error-gap-settings';settings.hidden=true;
      settings.innerHTML='<p><strong>Try a made-up gap between sizes.</strong> The first available size stays fixed. Following sizes move apart by your chosen amount.</p><div class="gap-controls"><label>Gap between sizes<input id="error-gap-value" type="number" min="0" step="any" value="3" inputmode="decimal"></label>'+selectMarkup('error-gap-unit','Gap unit',[['cm','Centimetres (cm)'],['in','Inches (in)']])+selectMarkup('error-gap-axis','Change the gap for',[['both','Waist and hip'],['waist','Waist only'],['hips','Hip only']])+'<button class="btn" id="error-gap-run" type="button">Run gap test</button></div><p id="error-gap-equivalent" class="gap-status"></p><p id="error-gap-status" class="gap-status" role="status"></p>';
      quick.querySelector('.quick-controls').after(settings);
    }
    filters.className='quick-table-filters';
    const resultLabel=$(mode+'-result').closest('label');
    for(const node of Array.from(resultLabel.childNodes))if(node.nodeType===3)node.textContent='Table result';
    $(mode+'-result').setAttribute('aria-label','Table result');
    if(mode==='cohort')filters.prepend(newLabel('cohort-table-model','Filter table by',[['v8','Our model'],['aiad','Aiad']]));
    const note=document.createElement('p');note.className='quick-table-note';note.textContent='Table-result filters only change the rows below. The percentage cards keep the full selected group as their denominator.';filters.after(note);
    // Put the detailed audit below the quick report, with one clear expandable entry.
    const details=document.createElement('details');details.className='full-results';details.innerHTML='<summary>Full tables, methodology and detailed checks</summary>';
    let sibling=quick.nextElementSibling;
    while(sibling){const next=sibling.nextElementSibling;details.appendChild(sibling);sibling=next;}
    quick.after(details);
    if(mode==='error'){
      for(const section of details.querySelectorAll('.section'))if(!section.querySelector('#error-decisions'))section.classList.add('real-chart-only');
      const simulated=document.createElement('section');simulated.className='section';simulated.id='error-simulated-charts';simulated.hidden=true;details.prepend(simulated);
    }
  }
  const dropdowns=[];
  function closeAll(except){for(const control of dropdowns)if(control!==except){control.menu.hidden=true;control.button.setAttribute('aria-expanded','false');}}
  function enhance(select){
    const label=labelForSelect(select),host=document.createElement('div'),button=document.createElement('button'),menu=document.createElement('div');
    host.className='custom-select';button.className='custom-select-trigger';button.type='button';button.id=select.id+'-trigger';button.setAttribute('role','combobox');button.setAttribute('aria-label',label);button.setAttribute('aria-haspopup','listbox');button.setAttribute('aria-expanded','false');button.setAttribute('aria-controls',select.id+'-listbox');
    menu.id=select.id+'-listbox';menu.className='custom-select-menu';menu.setAttribute('role','listbox');menu.setAttribute('aria-label',label+' choices');menu.hidden=true;
    const control={select,button,menu,host};dropdowns.push(control);
    const sync=()=>{button.innerHTML='<span>'+esc(select.selectedOptions[0]?.textContent||'Select')+'</span><span class="custom-select-chevron" aria-hidden="true">▼</span>';button.disabled=select.disabled;for(const option of menu.children)option.setAttribute('aria-selected',String(option.dataset.value===select.value));};
    const choose=option=>{select.value=option.value;select.dispatchEvent(new Event('change',{bubbles:true}));sync();closeAll();button.focus();};
    for(const option of select.options){const item=document.createElement('button');item.type='button';item.className='custom-option';item.setAttribute('role','option');item.dataset.value=option.value;item.disabled=option.disabled;item.textContent=option.textContent;item.addEventListener('click',event=>{event.preventDefault();choose(option);});menu.appendChild(item);}
    const open=()=>{closeAll(control);menu.hidden=false;button.setAttribute('aria-expanded','true');};
    button.addEventListener('click',event=>{event.preventDefault();menu.hidden?open():closeAll();});
    button.addEventListener('keydown',event=>{if(['ArrowDown','ArrowUp'].includes(event.key)){event.preventDefault();open();const options=Array.from(menu.children).filter(x=>!x.disabled);(options.find(x=>x.getAttribute('aria-selected')==='true')||options[0])?.focus();}else if(event.key==='Escape'){closeAll();}});
    menu.addEventListener('keydown',event=>{const options=Array.from(menu.children).filter(x=>!x.disabled),index=options.indexOf(document.activeElement);if(['ArrowDown','ArrowUp','Home','End'].includes(event.key)){event.preventDefault();const next=event.key==='Home'?0:event.key==='End'?options.length-1:(index+(event.key==='ArrowDown'?1:-1)+options.length)%options.length;options[next]?.focus();}else if(event.key==='Escape'){event.preventDefault();closeAll();button.focus();}else if(event.key==='Tab'){closeAll();}else if(event.key.length===1&&event.key!==' '){options.find(x=>x.textContent.toLowerCase().startsWith(event.key.toLowerCase()))?.focus();}});
    select.hidden=true;select.tabIndex=-1;select.setAttribute('aria-hidden','true');select.after(host);host.append(button,menu);select.addEventListener('change',sync);control.sync=sync;sync();
  }
  for(const select of document.querySelectorAll('select'))enhance(select);
  let appliedGap=null,gapUnit='cm';
  const gapDraft=()=>{const value=Number($('error-gap-value').value),gapCm=value*($('error-gap-unit').value==='in'?2.54:1);return {gapCm:Number(gapCm.toPrecision(12)),gapAxis:$('error-gap-axis').value,valid:$('error-gap-value').value.trim()!==''&&Number.isFinite(gapCm)&&gapCm>0};};
  const gapEquivalent=()=>{const draft=gapDraft();$('error-gap-equivalent').textContent=draft.valid?Number(draft.gapCm.toFixed(6))+' cm ≈ '+Number((draft.gapCm/2.54).toFixed(2))+' inches':'Enter a gap greater than zero.';};
  const gapChanged=()=>{appliedGap=null;gapEquivalent();$('error-gap-status').textContent='Click Run gap test to apply this gap.';document.dispatchEvent(new Event('report-gap-change'));};
  $('error-chart-mode').addEventListener('change',()=>{$('error-gap-settings').hidden=$('error-chart-mode').value!=='simulated';document.dispatchEvent(new Event('report-gap-change'));});
  $('error-gap-value').addEventListener('input',gapChanged);
  $('error-gap-axis').addEventListener('change',gapChanged);
  $('error-gap-unit').addEventListener('change',()=>{const value=Number($('error-gap-value').value),unit=$('error-gap-unit').value;if($('error-gap-value').value.trim()&&Number.isFinite(value))$('error-gap-value').value=String(Number((value*(gapUnit==='in'?2.54:1)/(unit==='in'?2.54:1)).toPrecision(15)));gapUnit=unit;gapChanged();});
  $('error-gap-run').addEventListener('click',()=>{const draft=gapDraft();if(!draft.valid){$('error-gap-status').textContent='Enter a gap greater than zero.';return;}appliedGap=draft;document.dispatchEvent(new Event('report-gap-change'));});
  gapEquivalent();
  document.addEventListener('click',event=>{if(!event.target.closest('.custom-select'))closeAll();});
  document.addEventListener('keydown',event=>{if(event.key==='Escape')closeAll();});
  $('reset-filters')?.addEventListener('click',()=>dropdowns.forEach(x=>x.sync()));
  const tally=(pairs,read)=>{const t={total:pairs.length,eligible:0,same:0,up:0,down:0,unavailable:0,unscorable:0,unaffected:0,adjacent:0};for(const entry of pairs){const pair=entry.pair||entry,value=read(entry);if(value===-2){t.unscorable++;continue;}if(value===-3){t.unaffected++;continue;}t.eligible++;if(value<0)t.unavailable++;else {t[value===pair[3]?'same':value>pair[3]?'up':'down']++;if(Math.abs(value-pair[3])===1)t.adjacent++;}}return t;};
  const bar=t=>'<div class="quick-bar" aria-hidden="true">'+['same','up','down','unavailable'].map(key=>'<span class="'+key+'" style="width:'+(t.eligible?100*t[key]/t.eligible:0)+'%"></span>').join('')+'</div>';
  const card=(title,t,style='',subtitle='exact same size',explanation='')=>'<article class="quick-card '+style+'"><h3>'+esc(title)+'</h3><div class="quick-result'+(explanation?' has-explanation':'')+'"><div><div class="quick-main">'+percent(t.same,t.eligible)+'</div><p class="quick-main-label">'+esc(subtitle)+'</p><p class="quick-denominator">'+count(t.same)+' of '+count(t.eligible)+(explanation?' product checks kept the same size':' valid checks')+'</p></div>'+explanation+'</div>'+bar(t)+'<div class="quick-breakdown">'+[['up','Size up','Chose a larger size, like M → L.'],['down','Size down','Chose a smaller size, like L → M.'],['unavailable','Unavailable','No matching size was available.']].map(([key,label,help])=>'<div><strong>'+percent(t[key],t.eligible)+'</strong><span>'+label+' · '+count(t[key])+'</span><p class="quick-outcome-help">'+help+'</p></div>').join('')+'</div><p class="quick-note">Exact or one size away: '+percent(t.same+t.adjacent,t.eligible)+'. This includes neighbouring sizes.</p></article>';
  const exclusions=t=>count(t.unscorable)+' without a valid reference'+(t.unaffected?' · '+count(t.unaffected)+' charts do not use the changed measurement':'')+'. These are excluded. Up and down mean larger or smaller than the real-measurement size.';
  const context=(mode,rows)=>{const gender=$(mode+'-gender').selectedOptions[0].textContent,category=$(mode+'-category').selectedOptions[0].textContent,people=new Set(rows.map(entry=>(entry.pair||entry)[0])).size,products=new Set(rows.map(entry=>(entry.pair||entry)[1])).size,search=$(mode+'-search').value.trim();return '<p class="quick-context"><strong>'+esc(gender)+' · '+esc(category)+'</strong> · '+count(people)+' people · '+count(products)+' products · '+count(rows.length)+' checks'+(search?' · Search: '+esc(search):'')+'</p>';};
  function measurementExplanation(rows,a,v){
    const summary=window.summarizeModelMeasurements(data,rows);
    const amount=cm=>cm==null?'N/A':Number(cm).toFixed(2)+' cm <small>≈ '+(cm/2.54).toFixed(2)+' in</small>';
    const models=[['aiad','Aiad'],['v8','Our model']],fields=[['waist','Waist'],['hips','Hip']];
    const table='<div class="table-wrap"><table id="model-measurement-errors"><thead><tr><th>Model</th><th>Measurement</th><th>Average error</th><th>Middle error (median)</th></tr></thead><tbody>'+models.flatMap(([model,label])=>fields.map(([field,name])=>{const stats=summary.models[model][field];return '<tr><td>'+label+'</td><td>'+name+'<small>'+count(stats.count)+' people with measurements</small></td><td>'+amount(stats.averageCm)+'</td><td>'+amount(stats.medianCm)+'</td></tr>';})).join('')+'</tbody></table></div>';
    let example='';
    if(summary.example){
      const pair=summary.example,person=data.people[pair[0]],product=data.products[pair[1]],sizes=product.chart.orderedSizes;
      const cm=value=>value==null?'N/A':value.toFixed(2)+' cm';
      example='<div class="example"><h4>One real example from these checks</h4><p>'+esc(person.scanId)+' · <a href="'+esc(product.url)+'" target="_blank" rel="noopener noreferrer">'+esc(product.title)+'</a></p><div class="table-wrap"><table id="model-error-example"><thead><tr><th>Measurements from</th><th>Waist</th><th>Hip</th><th>Size chosen</th></tr></thead><tbody>'+[['Real tape',person.actuals,pair[3]],['Aiad',person.predictions.aiad,pair[4][0]],['Our model',person.predictions.v8,pair[5][0]]].map(([label,tape,index])=>'<tr><td>'+label+'</td><td>'+cm(tape.waist)+'</td><td>'+cm(tape.hips)+'</td><td>'+esc(sizes[index])+'</td></tr>').join('')+'</tbody></table></div><p>The same person and product get different sizes because the model measurements differ from the real tape. This is one recorded example, not an average.</p></div>';
    }
    return '<section class="model-error-explanation"><h3>Why do the sizes differ?</h3><p><strong>A few centimetres can move someone into the next size.</strong> Both models have measurement errors. The current sizing rule gives waist priority, so waist errors are especially important. Larger errors can also leave no matching size available.</p>'+table+'<p class="quick-note"><strong>Average error</strong> is the average distance from real tape. <strong>Middle error (median)</strong> means half the measured people have an error at or below this value. Errors can be above or below the tape measurement.</p><p class="quick-note">Each person is counted once in this table. Missing measurements are left out. The size percentages above count person-and-product checks, so they have a different total.</p><p class="quick-takeaway">Including the neighbouring size, Aiad reaches '+percent(a.same+a.adjacent,a.eligible)+' and our model reaches '+percent(v.same+v.adjacent,v.eligible)+'. Those neighbouring sizes still count as mistakes for exact-size matching.</p><p class="quick-note">A lower average error does not guarantee the right size for every product. How close the person is to the next size and which sizes are available also matter. These results test chart choices; actual garment fit is not measured.</p>'+example+'</section>';
  }
  function renderCohort(rows){
    const a=tally(rows,pair=>pair[4]?.[0]??-2),v=tally(rows,pair=>pair[5]?.[0]??-2);
    $('cohort-quick-summary').innerHTML=context('cohort',rows)+(rows.length?'<p class="quick-context"><strong>Real model predictions · real product charts.</strong> No extra tape error is added in this comparison.</p><div class="quick-model-grid">'+card('Aiad',a)+card('Our trained model',v,'our')+'</div><p class="quick-takeaway">Aiad chooses the exact reference size in '+percent(a.same,a.eligible)+' of checks. Our trained model does so in '+percent(v.same,v.eligible)+'.</p><p class="quick-note">'+exclusions(a)+'</p>'+measurementExplanation(rows,a,v):'<div class="quick-empty">No matching products or people. Try another selection.</div>');
  }
  function renderError(rows,scenario,activeData=data){
    const t=tally(rows,entry=>scenario.sizeIndices[entry.index]);
    const selectedPeople=[...new Set(rows.map(entry=>entry.pair[0]))].map(index=>data.people[index]);
    const selectedProducts=[...new Set(rows.map(entry=>entry.pair[1]))].map(index=>data.products[index]);
    const countedPeople=new Set(rows.filter(entry=>scenario.sizeIndices[entry.index]>=-1).map(entry=>entry.pair[0])).size;
    const explanation='<aside class="quick-check-explanation" aria-label="People, products and checks"><p><strong>'+count(selectedPeople.length)+' people · '+count(selectedProducts.length)+' different products</strong><br>'+count(selectedProducts.filter(p=>p.groupId.startsWith('female-')).length)+' women’s products + '+count(selectedProducts.filter(p=>p.groupId.startsWith('male-')).length)+' men’s products</p><p><strong>1 person + 1 product = 1 check.</strong><br>The same products are checked with many people.</p><p><strong>'+count(t.eligible)+' checks were counted.</strong><br>These checks came from '+count(countedPeople)+' people.</p></aside>';
    const error=dualUnit(scenario.deltaCm,true),axis=$('error-axis').selectedOptions[0].textContent;
    const allErrors=new Map(activeData.scenarios.filter(s=>s.axis===scenario.axis).map(s=>[s.deltaCm,tally(rows,entry=>s.sizeIndices[entry.index])]));
    let largest=0;for(let cm=1;cm<=7;cm++){const negative=allErrors.get(-cm),positive=allErrors.get(cm);if(!negative.eligible||!positive.eligible||negative.same/negative.eligible<.9||positive.same/positive.eligible<.9)break;largest=cm;}
    const guide=!t.eligible?'No valid checks for this selection.':largest?('For a 90% same-size target, every tested whole-centimetre error from −'+largest+' to +'+largest+' cm passes for this selection.'):'For a 90% same-size target, ±1 cm already falls below the target in at least one direction. No larger error range is established by these whole-centimetre tests.';
    const comparison='<h3>All errors for your selected people and products</h3><div class="table-wrap"><table id="error-range-summary"><thead><tr><th>Error amount (cm / inches)</th><th>Smaller tape (−): same size</th><th>Larger tape (+): same size</th></tr></thead><tbody>'+[0,1,2,3,4,5,6,7].map(cm=>{const minus=allErrors.get(-cm),plus=allErrors.get(cm);return '<tr><td>'+dualUnit(cm)+(cm===0?' · baseline':'')+'</td><td>'+percent(minus.same,minus.eligible)+' · '+count(minus.same)+' / '+count(minus.eligible)+'</td><td>'+percent(plus.same,plus.eligible)+' · '+count(plus.same)+' / '+count(plus.eligible)+'</td></tr>';}).join('')+'</tbody></table></div><p class="quick-note">Inches are rounded for display; calculations use the exact centimetre values (1 inch = 2.54 cm). Each direction is tested separately. For both measurements, waist and hip move together. This guide describes the tested size-chart decisions, not a guaranteed fit limit for every person or product.</p>';
    const simulation=activeData.simulation?'<p class="simulation-label"><strong>Made-up chart simulation · '+dualUnit(activeData.simulation.gapCm)+' gap</strong>Gap changed for '+({both:'waist and hip',waist:'waist',hips:'hip'}[activeData.simulation.gapAxis])+'. These are hypothetical size charts. Both the starting size and the size after error are recalculated using these charts.</p>':'';
    $('error-quick-summary').innerHTML=simulation+context('error',rows)+(rows.length?'<p class="quick-context"><strong>'+esc(axis)+' · '+esc(error)+'</strong> applied to the real WEAR tape measurements.</p>'+card('Product size after tape error',t,'','keep the same product size',explanation)+'<p class="quick-takeaway">'+(scenario.deltaCm===0?'0 cm changes nothing: this is the unchanged baseline.':('With '+esc(error)+', '+percent(t.same,t.eligible)+' keep the same size, '+percent(t.up,t.eligible)+' go up, '+percent(t.down,t.eligible)+' go down and '+percent(t.unavailable,t.eligible)+' become unavailable.'))+'</p><p class="quick-note">'+count(t.unscorable)+' checks were not counted because tape measurements were missing or no '+(activeData.simulation?'size in the made-up chart':'stocked size')+' matched the real tape.'+(t.unaffected?' Another '+count(t.unaffected)+' checks were excluded because the chart does not use the changed measurement.':'')+'</p>'+(scenario.axis==='hips'||activeData.simulation?.gapAxis==='hips'?'<p class="quick-note">The current sizing rule prioritises waist. A high hip-only same-size rate does not prove physical garment fit.</p>':'')+'<p class="quick-takeaway">'+guide+'</p>'+comparison:'<div class="quick-empty">No matching products or people. Try another selection.</div>');
  }

  return {renderCohort,renderError,getGapRequest:()=>({active:$('error-chart-mode').value==='simulated',applied:appliedGap}),sync:()=>dropdowns.forEach(x=>x.sync()),closeAll};
}

module.exports={controlsCss,installReportControls};
